# 万豪高级 DevOps 工程师 — 笔试任务拆解与执行计划

> 收到题目日期：两天内完成。本文档先理清「要做什么、怎么拆、优先级、技术选型」，避免一上来就写代码走弯路。

---

## 一、题目到底在考什么

一句话：**用「代码即一切（Everything as Code）」的方式，把一个多层应用，从基础设施到 K8s 部署到 CI/CD 流水线，完整地交付出来，覆盖 5 套环境。**

三大考察方向（也是岗位画像）：

| 方向 | 对应 Task | 权重 |
|------|-----------|:--:|
| 基础设施（IaC） | Task 1（Terraform） | 🟡 面试官说**不是重点** |
| Kubernetes / 容器 | Task 2（Manifests） | 🔴 **重点** |
| 流水线与自动化 | Task 3（CI/CD） | 🔴 **重点** |

**核心主线（贯穿全部 Task）**：同一个应用，要能部署到 5 套环境：

```
dev → test → perf → staging → production
开发    测试    压测     预发布     生产
```

> ⚠️ 5 套环境是整套题目的灵魂。面试官看的是「你怎么用一套代码管 5 个环境」，而不是「你能不能部署一次」。

---

## 二、交付物清单（先明确最终要交什么）

### 必须交付

- [ ] 一个 Git 仓库（公开仓库，或 Git 压缩包），包含全部代码
- [ ] 每个 Task 的提交记录 + 提交说明（体现「代码即一切」）

### 可选交付（做了能加分，但不强求）

- [ ] 部署好的服务公网链接 / 截图
- [ ] Terraform Cloud 截图
- [ ] CI/CD 流水线截图

> 💡 题目补充提示第 3 条明确说了：**「不用真的花钱上云跑完整生产，代码逻辑完整即可，面试主要看代码结构、架构思路」**。所以我们的目标是**代码完整、结构清晰、能讲清楚**，不是真跑通生产。

---

## 三、Task 逐个拆解

### Task 0：项目代码库（先做，是所有后续 Task 的前提）

**要求**：一个「多层架构」的开源项目，或自己建一个多层 Hello-World。

**建议**：自己建一个**三层架构**的简单项目，最可控，也最容易讲清楚架构：

```
┌─────────────────────────────────────────┐
│  前端 (frontend)  静态页面 / Nginx        │  ← 展示层
├─────────────────────────────────────────┤
│  后端 (backend)   API 服务 (Go/Java/Node)│  ← 业务层
├─────────────────────────────────────────┤
│  数据层  PostgreSQL + Redis + OSS/S3     │  ← 数据层
└─────────────────────────────────────────┘
```

**为什么推荐自己建而不是用开源项目**：
1. 2 天时间紧，开源大项目要花大量时间理解它的构建/依赖
2. 自己建的最简单，能 100% 掌控 Dockerfile、依赖、端口
3. 三层结构清晰，正好对应 Task 1 的「关系型数据库 + Redis + 对象存储」三种存储

**技术选型建议**：
- 后端：你熟悉 Go（有 golang-operator 教程），一个 HTTP API + 一个 DB 连接 + 一个 Redis 连接
- 前端：一个静态页面（Nginx 托管），或者干脆用后端返回 JSON 也算一层
- 数据库：PostgreSQL（关系型）
- 缓存：Redis
- 对象存储：OSS/S3（Task 1 用到，业务里可以不真连）

> 如果时间实在不够，前端可以是一个最简单的 index.html，重点是「分层结构清晰」。

### Task 1：基础设施即代码（IaC）— 🟡 不是重点，做到代码完整即可

**技术栈**：Terraform + 阿里云（优先）

**要编排的组件**：

| 组件 | 是否必须 | 说明 |
|------|:--:|------|
| VPC | 选做 | 假设网络已就绪 |
| 负载均衡 SLB | 选做 | |
| K8s 集群 ACK | 选做 | 假设计算已就绪 |
| **关系型数据库 RDS** | 至少一种 | ✅ 必选 |
| **Redis** | 至少一种 | ✅ 必选 |
| **OSS 对象存储** | 至少一种 | ✅ 必选 |
| 容器镜像仓库 ACR | 选做 | 用公共仓库可省 |

**5 套环境怎么处理**：Terraform workspace 区分环境。

```
terraform workspace 的 5 个环境：
  dev / test / perf / staging / prod

通过 variables.tf + 每个 workspace 的 tfvars 区分配置
（比如 dev 用小规格 RDS，prod 用高可用 RDS）
```

**评分标准（面试官视角）**：不是看你真不真跑通，是看：
1. 目录结构是否清晰（modules 模块化）
2. 是否用 variables 参数化（而非硬编码）
3. 5 套环境是否用 workspace 优雅区分
4. 是否有 backend 远程状态管理

> 注意：Task 1 面试官明确说不是重点，**先保证「代码完整 + 结构合理」，不要花太多时间在 Terraform 细节上**。把省下来的时间投给 Task 2 和 Task 3。

### Task 2：Kubernetes — 🔴 重点，重点打磨

**必须包含**：
- [ ] Deployment
- [ ] Service
- [ ] Ingress

**加分项（至少实现一项）**：
- [ ] HPA 水平自动扩缩容
- [ ] cert-manager 证书管理

**核心难点：同一套服务部署到 5 套环境**

推荐方案：**Kustomize 的 overlay 模式**（这是业界标准做法，面试加分）：

```
k8s/
├── base/                        ← 共用的基础 manifests
│   ├── deployment.yaml
│   ├── service.yaml
│   ├── ingress.yaml
│   ├── hpa.yaml
│   └── kustomization.yaml
├── overlays/
│   ├── dev/                     ← 每个环境一个 overlay
│   │   ├── kustomization.yaml   （改 namespace、镜像 tag、副本数、域名）
│   │   └── ...
│   ├── test/
│   ├── perf/
│   ├── staging/
│   └── production/
```

**每个环境 overlay 的差异点**（体现你懂环境管理）：

| 差异项 | dev | test | perf | staging | production |
|--------|-----|------|------|---------|-----------|
| namespace | dev | test | perf | staging | production |
| 副本数 | 1 | 1 | 3（压测） | 2 | 3+ |
| 镜像 tag | latest | 测试版 | 压测版 | 预发布版 | 稳定版 |
| Ingress 域名 | dev.xxx | test.xxx | perf.xxx | staging.xxx | xxx.com |
| HPA | 关 | 关 | 开 | 开 | 开 |

**这 5 套环境的差异设计，是 Task 2 最能体现能力的地方。**

> 对比 Terraform 的 workspace 概念和 K8s 的 namespace/overlay 概念，面试时能讲清楚「为什么 IaC 用 workspace、K8s 用 namespace」就是加分项。

### Task 3：流水线即代码 — 🔴 重点

**3.1 基础设施流水线（选做）**：可以做个简化的 Terraform pipeline，plan → 审批 → apply。时间紧可以只写配置不完整跑通。

**3.2 业务服务流水线（必做）**：

```
代码提交 → 构建镜像 → 推送仓库 → 部署到环境
         （集成质量/安全扫描，选做）
```

**CI/CD 工具选择**：题目优先 Harness，但你熟悉 GitLab CI（有教程），**选 GitLab CI 或 GitHub Actions 都行**。建议用 GitHub Actions（免费、公开仓库天然支持、面试官好查看）。

**流水线阶段设计**：

```
Stage 1: 构建 + 测试
  ├── 编译后端
  ├── 单元测试（选做）
  └── 代码质量扫描（选做）

Stage 2: 构建镜像 + 安全扫描
  ├── docker build
  ├── Trivy 安全扫描（选做）
  └── push 到镜像仓库

Stage 3: 部署
  ├── 自动部署到 dev / test
  ├── 人工审批后部署 staging / prod（体现"分级发布"）
  └── 蓝绿/金丝雀 + 自动回滚（选做加分）
```

**最体现能力的点**：
1. 环境分级的审批流（dev/test 自动，prod 需审批）
2. 镜像 tag 策略（用 commit SHA，不用 latest）
3. 部署到 5 套环境用参数区分（和 Task 2 的 overlay 对应）

### Task 4：文档即代码

三张图：
1. **应用架构设计图** — 三层应用（前端/后端/数据层）
2. **基础设施架构设计图** — VPC/SLB/RDS/Redis/OSS/ACK
3. **CI/CD 流水线架构图** — 构建/扫描/部署/审批

> 图可以用 Mermaid（纯代码，符合「代码即一切」，且能直接进 Git）或 draw.io 源文件。**推荐 Mermaid**——它本身就是代码，放进 README 就能渲染，最能体现「Everything as Code」。

---

## 四、两天执行计划（时间分配）

面试官说 Task 1 不是重点，所以按这个优先级：

| 时间 | 做什么 | 产出 |
|------|--------|------|
| **第 1 天上午** | Task 0：建项目代码库 | 三层应用 + Dockerfile + 能本地跑 |
| **第 1 天下午** | Task 2：K8s manifests（重点） | base + 5 套 overlay + HPA |
| **第 2 天上午** | Task 3：CI/CD 流水线（重点） | 构建→部署流水线 + 环境分级 |
| **第 2 天下午** | Task 1（简化）+ Task 4 文档 | Terraform 代码完整 + 3 张图 |

**如果时间不够的取舍顺序**：
1. 先保证 Task 2（K8s）完整 —— 这是必做重点
2. 再保证 Task 3（CI/CD）完整 —— 必做重点
3. Task 1（Terraform）做到「结构完整、逻辑正确」，不纠结细节
4. Task 4 的 3 张图用 Mermaid 快速画

---

## 五、关键决策点（需要你确认的）

开始写代码前，有几个选型需要定，避免中途返工：

### 1. Task 0 项目选型
- [ ] 方案 A：自己建一个三层 Hello-World（推荐，最可控）
- [ ] 方案 B：用某个开源多层项目

### 2. CI/CD 平台
- [ ] GitHub Actions（公开仓库好展示）
- [ ] GitLab CI（你熟悉）
- [ ] 其他

### 3. K8s 环境管理
- [ ] Kustomize overlay（推荐，业界标准）
- [ ] Helm（你也熟）

### 4. Terraform 云厂商
- [ ] 阿里云（题目优先）
- [ ] AWS
- [ ] 混合（多区域多厂商，加分项）

---

## 六、一句话总结

**这套题的核心不是「能不能部署」，是「怎么用代码管理一个应用从基础设施到部署到发布的完整生命周期，并且优雅地覆盖 5 套环境」。**

面试官想看到的是：
1. 代码结构清晰（模块化、参数化、不硬编码）
2. 5 套环境的处理方式专业（workspace + namespace/overlay）
3. 流水线有分级发布意识（自动 vs 审批）
4. 一切都能讲清楚为什么这么设计

---

> 下一步：你确认上面「关键决策点」的几个选型，我就开始帮你搭 Task 0 的项目骨架。
