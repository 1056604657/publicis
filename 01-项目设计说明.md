# 三层 Hello-World 项目设计说明

> 在写代码之前，先把「这个项目需要有什么」彻底想清楚。这份文档就是 Task 0 的设计蓝图，也是 Task 4 应用架构图的文字版。

---

## 一、架构总览（三层）

```
┌──────────────────────────────────────────────────────────┐
│  第 1 层：前端 (frontend)                                 │
│  Nginx 托管静态页面 + 反向代理 /api 到后端                  │
│  技术：HTML/JS + Nginx                                    │
├──────────────────────────────────────────────────────────┤
│  第 2 层：后端 (backend)                                  │
│  Go 编写的 REST API 服务                                  │
│  技术：Go + net/http（标准库，零依赖，最省事）              │
├──────────────────────────────────────────────────────────┤
│  第 3 层：数据层 (data layer)                             │
│  PostgreSQL（持久化）+ Redis（缓存）+ OSS（对象存储，可选）│
└──────────────────────────────────────────────────────────┘
```

**为什么选这个设计**：
1. 三层结构清晰，面试时能画出架构图、讲清数据流
2. 后端用 Go 标准库，Dockerfile 多阶段构建，镜像小（体现懂 Go + 懂容器优化）
3. Redis 缓存 + PostgreSQL 持久化，体现「有状态服务」的处理（这是 K8s 面试重点）
4. 覆盖 Task 1 要求的三种存储：RDS（PostgreSQL）、Redis、OSS

---

## 二、每一层需要有什么

### 第 1 层：前端（frontend）

| 文件 | 作用 |
|------|------|
| `index.html` | 静态页面，展示商品列表 |
| `app.js` | 调后端 API（fetch `/api/items`） |
| `nginx.conf` | 托管静态文件 + `location /api` 反向代理到后端 |
| `Dockerfile` | 基于 nginx 镜像，拷贝静态文件和配置 |

**为什么前端也要 Nginx 反向代理**：前端页面调 `/api/items` 时，由 Nginx 转发到后端 Service。这样浏览器只访问一个入口，不存在跨域问题。这是生产级前端的标准做法，面试能讲。

### 第 2 层：后端（backend）

| 文件 | 作用 |
|------|------|
| `main.go` | 入口，注册路由 |
| `go.mod` | Go module（零第三方依赖，用标准库） |
| `Dockerfile` | 多阶段构建：golang 编译 → alpine 运行 |

**后端提供的 API**：

| 端点 | 方法 | 作用 | 对应 K8s 概念 |
|------|------|------|--------------|
| `/healthz` | GET | 健康检查 | 存活/就绪探针 |
| `/readyz` | GET | 就绪检查（检查 DB/Redis 连通） | 就绪探针 |
| `/api/items` | GET | 查询列表（先查 Redis，miss 查 DB） | 业务接口 |
| `/api/items` | POST | 新增（写 DB，失效缓存） | 业务接口 |

**为什么要有 /healthz 和 /readyz 两个探针**（面试重点）：
- `/healthz`（存活探针）：进程还活着吗？只检查进程，不检查依赖
- `/readyz`（就绪探针）：能对外服务了吗？检查 DB + Redis 是否连通

> 这两者分离是 K8s 生产实践的关键：DB 挂了应该「就绪探针失败，摘流量」，而不是「存活探针失败，重启容器」——重启容器也解决不了 DB 挂了的问题。

### 第 3 层：数据层

| 组件 | 作用 | 对应 Task 1 |
|------|------|------------|
| **PostgreSQL** | 持久化存储商品数据 | RDS |
| **Redis** | 缓存商品列表 | Redis |
| **OSS** | 对象存储（Terraform 里创建，业务代码不真连） | OSS |

**数据库表**（`db/init.sql`）：

```sql
CREATE TABLE IF NOT EXISTS items (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  description TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

INSERT INTO items (name, description) VALUES
  ('示例商品1', 'Hello World 项目示例数据'),
  ('示例商品2', '用于验证三层架构');
```

---

## 三、数据流（面试要能讲清这张图）

```
浏览器 → 前端 Nginx
           │  /api/items
           ▼
        后端 Go API
           │  ① 先查 Redis
           ▼
        Redis 缓存
           │  miss（没命中）
           ▼
        PostgreSQL
           │  ② 查到数据
           ▼
        写回 Redis（下次命中）
           │
           ▼
        返回 JSON 给前端
```

**这条数据流体现了什么**（面试加分点）：
1. 缓存穿透、缓存更新的经典场景
2. 「有状态」的 Redis 和 PostgreSQL 需要持久化（对应 K8s PVC）
3. 健康检查要区分「进程活着」vs「依赖连通」

---

## 四、环境变量设计（12-factor，贯穿 5 套环境）

后端通过环境变量读取配置，**不硬编码**。这直接对应 K8s 的 ConfigMap/Secret 注入：

| 环境变量 | 作用 | 5 套环境差异 |
|----------|------|-------------|
| `DB_HOST` | PostgreSQL 地址 | 每环境指向自己的 RDS |
| `DB_PORT` | PostgreSQL 端口 | 通常 5432 |
| `DB_USER` | 数据库用户 | 每环境独立 |
| `DB_PASSWORD` | 数据库密码 | Secret 注入 |
| `DB_NAME` | 数据库名 | 通常相同 |
| `REDIS_HOST` | Redis 地址 | 每环境指向自己的 Redis |
| `REDIS_PORT` | Redis 端口 | 通常 6379 |
| `APP_PORT` | 后端监听端口 | 通常 8080 |

> 12-factor 原则：配置和代码分离。代码里只读 `os.Getenv("DB_HOST")`，值由 K8s 的 ConfigMap/Secret 注入。这是「一套代码部署 5 套环境」的基础。

---

## 五、完整文件清单（最终要交付的东西）

```
marriott-devops/
├── 00-任务拆解与执行计划.md          ← 已写
├── 01-项目设计说明.md                ← 本文件
├── README.md                        ← 仓库根说明（Task 4 部分）
│
├── src/                             ← Task 0：应用源码
│   ├── backend/
│   │   ├── main.go
│   │   ├── go.mod
│   │   ├── Dockerfile
│   │   └── db/init.sql              （或放 src/db/）
│   └── frontend/
│       ├── index.html
│       ├── app.js
│       ├── nginx.conf
│       └── Dockerfile
│
├── k8s/                             ← Task 2：K8s manifests
│   ├── base/
│   │   ├── kustomization.yaml
│   │   ├── backend-deployment.yaml
│   │   ├── backend-service.yaml
│   │   ├── frontend-deployment.yaml
│   │   ├── frontend-service.yaml
│   │   ├── ingress.yaml
│   │   ├── hpa.yaml
│   │   ├── postgres-statefulset.yaml
│   │   ├── redis-deployment.yaml
│   │   └── configmap.yaml
│   └── overlays/
│       ├── dev/  test/  perf/  staging/  production/
│       │   └── kustomization.yaml
│
├── terraform/                       ← Task 1：IaC
│   ├── main.tf
│   ├── variables.tf
│   ├── outputs.tf
│   ├── modules/
│   │   ├── rds/
│   │   ├── redis/
│   │   └── oss/
│   └── environments/
│       ├── dev.tfvars
│       ├── test.tfvars
│       ├── perf.tfvars
│       ├── staging.tfvars
│       └── production.tfvars
│
├── .gitlab-ci.yml                   ← Task 3：流水线
│
└── docs/                            ← Task 4：架构图（Mermaid）
    ├── application-architecture.md
    ├── infrastructure-architecture.md
    └── cicd-pipeline-architecture.md
```

---

## 六、几个关键设计决策（为什么这么做）

### 1. 后端用 Go 标准库，不引入框架

- 零第三方依赖，Dockerfile 最简单，编译最快
- 多阶段构建后镜像 < 20MB（体现容器优化能力）
- 你有 Go 基础（golang-operator 教程），能看懂能改

### 2. 数据库和 Redis 用 StatefulSet / Deployment 跑在 K8s 里

- 为了「代码即一切」，测试环境里 DB/Redis 也跑在 K8s（dev/test 环境）
- 生产环境（staging/prod）指向 Task 1 建的阿里云 RDS/Redis（体现「云资源 vs 集群内资源」的分层）

> 这是一个很加分的点：**dev/test 环境用集群内的轻量 DB，staging/prod 用云上托管 RDS**，说明你懂「不同环境不同资源策略」。

### 3. 前端 Nginx 反向代理 /api

解决跨域，生产级做法。

---

## 七、这个设计能覆盖题目所有要求

| 题目要求 | 本项目如何覆盖 |
|----------|---------------|
| 多层架构 | 前端 / 后端 / 数据层三层 |
| 关系型数据库 | PostgreSQL |
| NoSQL / Redis | Redis |
| S3/OSS | OSS（Terraform 创建） |
| 5 套环境 | Terraform workspace + K8s namespace/overlay |
| Deployment/Service/Ingress | k8s/base 里全有 |
| HPA | hpa.yaml（perf/staging/prod 环境开启） |
| 构建→部署流水线 | .gitlab-ci.yml |
| 3 张架构图 | docs/ 里 Mermaid |

---

## 八、下一步

确认这个设计没问题，我就按顺序开始写：

1. **Task 0**：先写后端（main.go + go.mod + Dockerfile），再写前端，最后本地验证能跑
2. 然后进入 **Task 2**（K8s manifests，重点）

如果你对某个设计点有不同想法（比如后端不用 Go、或者 DB 不进 K8s），现在提，改起来成本最低。
