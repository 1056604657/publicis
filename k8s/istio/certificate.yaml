apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: marriott-tls
  namespace: istio-system
spec:
  secretName: marriott-tls
  dnsNames:
    - app.marriott.com
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
