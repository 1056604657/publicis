server {
    listen 8080;
    server_name _;

    # 前端静态文件
    root /usr/share/nginx/html;
    index index.html;

    # 反向代理 /api 到后端 Service（避免跨域）
    # 注意：proxy_pass 不能带尾部斜杠，否则 nginx 会剥掉 /api/ 前缀，
    #       /api/items 会变成 /items 转发到后端导致 404
    location /api/ {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 前端健康检查（供 Ingress / 探针使用）
    location /healthz {
        return 200 'ok';
        add_header Content-Type text/plain;
    }
}
